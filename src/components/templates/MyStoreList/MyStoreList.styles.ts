import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  center: {
    marginHorizontal: 'auto',
  },
  errorText: {
    fontSize: 24,
    color: '#C13333',
  },
  emptyText: {
    fontSize: 24,
    color: '#fff',
  },
});
