import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#171f26',
    paddingHorizontal: 12,
    paddingVertical: 12,
    height: 48,
    borderRadius: 12,
    marginBottom: 12,
    color: '#FFFFFF',
  },
});
