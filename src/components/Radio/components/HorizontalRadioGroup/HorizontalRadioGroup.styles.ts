import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  horizontalRadioGroup: {
    flexDirection: 'row',
    gap: 24,
  },
  radioItem: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
});
