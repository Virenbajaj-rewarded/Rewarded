import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    padding: 16,
  },
  headerTitle: {
    fontSize: 24,
    marginBottom: 16,
  },
  loginText: {
    fontSize: 12,
    color: '#3c83f6',
  },
});
