import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    width: "100%",
    maxWidth: 420,
    marginHorizontal: "auto",
    borderRadius: 12,
    padding: 20,
  },
  inputWrapper: {
    marginBottom: 8,
  },
});
